init_config:
  is_jmx: true
  collect_default_metrics: true

instances:
  - host: localhost
    port: 18983
    rmi_registry_ssl: true

logs:
  - type: file
    path: /usr/local/solr/logs/*.log
    source: solr
    service: app-java